{
  "name": "madrassa-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "Fullstack Madrassa Management System with React + Express + MongoDB Atlas",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/server.js",
    "server": "node server/server.js",
    "client": "npm run dev --prefix client",
    "seed": "node server/seed.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.6.0",
    "multer": "^1.4.5-lts.1"
  }
}
